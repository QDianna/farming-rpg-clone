using UnityEngine;

public class PlotlandTrigger : MonoBehaviour
{
    /*  TODO    - unlocked land: share farming instructions in UI (till, plant, wait, harvest)
        TODO    - locked land: share instructions in UI on how to buy land, cost etc
    */ 
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
